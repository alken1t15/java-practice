package Nikita;

import java.util.ArrayList;

public class Farm {
    /*
    ArrayList<Farm> hoof = new ArrayList<>();
    ArrayList<Farm> birdFarms = new ArrayList<>();
    public  void addAnimal(Farm farm ,Animal arr){
        if(farm instanceof  HoofFarm){
            hoof.add(arr)
        }
        arr.walk();
    }
    public  void rollCall(){
        for(int i = 0; i< 4; i++){
            int random = (int)(Math.random()* array.size());
            array.get(random).say();
            array.remove(random);
        }
    }

     */
}
